import  "../App.css";

function Navbar(){
    
    return ( 
        <nav>
            <div className="logo">Logo</div>
            <div className="nav-links">
                <a href="#"> Home</a>
                <a href="#">About us</a>
                <a href="#">Categories</a>
            </div>

        </nav>
    );
}

export default Navbar